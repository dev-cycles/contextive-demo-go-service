contexts:
  - name: Demo
    domainVisionStatement: To demonstrate storing definitions in a common repo and sharing with other repos via native package management
    paths:
    # Globs are supported. Multiple paths may be included. If any match the currently open file, the context will be used.
    - "**" # This is the default path glob if the `paths` key is excluded, and will match all files
    terms:
    - name: sample
      definition: A sample term used in this demo
      examples:
        - I really like the taste of these samples. 😍